{
  "name": "qa-messenger-test-task",
  "version": "1.0.0",
  "private": true,
  "description": "QA Engineer test task for web messenger message sending flow",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:headed": "playwright test --headed",
    "test:e2e:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.54.0"
  }
}
